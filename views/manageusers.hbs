  
            <div class="container-fluid" style="padding-bottom: 70px;">
                {{> messages}}

                {{!-- Nav Tabs --}}
                <div class="card">
                    <div class="card-header">
                        {{!-- <ul class="nav nav-tabs card-header-tabs">
                            <li class="nav-item btn"><a id="tab" class="nav-link active"  onclick="opentab('users')">Users</a></li>
                            <li class="nav-item btn"><a id="tab" class="nav-link"  onclick="opentab('reguser')">Register User</a></li>
                            <li class="nav-item"></li>
                        </ul> --}}

                            <ul class="nav nav-tabs" role="tablist">
                            <li class="nav-item">
                            <a class="nav-link active" data-toggle="tab" href="#users">Users</a>
                            </li>

                            <li class="nav-item">
                            <a class="nav-link" data-toggle="tab" href="#reguser">Register User</a>
                            </li>

                            </ul>
                    </div>
                    
                    {{!-- Tab panes --}}

                      <div class="tab-content">
                        {{!-- ALL USERS --}}
                        <div id="users" class="container tab-pane active"><br>
                        <div class="row">
                            <div class="col-md-6 text-nowrap">
                                {{!-- <div id="dataTable_length" class="dataTables_length" aria-controls="dataTable"><label style="padding: 11px;margin: -6px;margin-right: 20px;">Branch 
                                    <select class="form-control form-control-sm custom-select custom-select-sm" style="padding-left: 10 px;margin-left: 20;" id="filter" onkeyup="filter()">
                                        <option value="10" selected>Branch A</option>
                                        <option value="Branch B">Branch B</option>
                                        <option value="Branch C">Branch C</option>
                                        <option value="Branch D">Branch D</option>
                                        <option value="Branch D">Branch D</option>
                                        <option value="Branch D">Branch D</option>
                                    </select></label>
                                </div> --}}

                                {{!-- FILTER TABLE --}}
                                <div class="dataTables_filter" id="dataTable_filter"> Branch: <label> 
                                    <input type="text" class="form-control form-control-sm" 
                                        id="filter" onkeyup="filter()" placeholder="All Branches"></label>
                                </div>
                                {{!-- END FILTER --}}
                               
                            </div>
                         <div style="padding-bottom: 10px;"> </div>
                        </div>
                        <div class="table-responsive table mt-2" id="dataTable" role="grid" aria-describedby="dataTable_info" 
                            {{!-- style=" overflow-y:scroll; height:610px; display:block;"> --}}>
                            <table class="table my-0" id="usersTable">
                                <thead>
                                    <tr>
                                        <th onclick="sortTable(0)"> Name <i class="fa fa-fw fa-sort"></i></th>
                                        <th onclick="sortTable(1)">Role <i class="fa fa-fw fa-sort"></i></th>
                                        <th onclick="sortTable(2)">Branch <i class="fa fa-fw fa-sort"></i></th>
                                        <th >Mobile Number </th>
                                        <th onclick="sortTable(4)">Email <i class="fa fa-fw fa-sort"></i></th>
                                    </tr>
                                </thead>
                                <tbody>
                                   {{>userList}}
                                </tbody>
                            
                            </table>
                        </div>
                        {{!-- <div class="row">
                            <div class="col-md-6 align-self-center">
                                <p id="dataTable_info" class="dataTables_info" role="status" aria-live="polite">Showing 1 to 10</p>
                            </div>
                            <div class="col-md-6">
                                <nav class="d-lg-flex justify-content-lg-end dataTables_paginate paging_simple_numbers">
                                    <ul class="pagination">
                                        <li class="page-item disabled"><a class="page-link" href="#" aria-label="Previous"><span aria-hidden="true">«</span></a></li>
                                        <li class="page-item active"><a class="page-link" href="#">1</a></li>
                                        <li class="page-item"><a class="page-link" href="#" aria-label="Next"><span aria-hidden="true">»</span></a></li>
                                    </ul>
                                </nav>
                            </div>
                        </div> --}}
                    </div>
                    {{!-- END OF ALL USERS --}}

                    {{!-- Register a User --}}
                    <div id="reguser" class="container tab-pane fade"><br>
                         <h3 class="text-dark mb-4">Register a User</h3>
                        <div class="row">
                            <div class="col-md-6">
                                <div class="up_profpic">
                                  <div >
                                    <img class="rounded-circle mb-3 mt-4" src="assets/img/icons/guest.jpg" width="250" height="250" />
                                {{!-- Start form --}}
                                <form oninput='password.setCustomValidity(up2.value != up.value ? "Passwords do not match." : "")' method="POST" action="/register">    
                                    
                                  </div> 
                                </div>
                            </div>

                            <div class="col" id="regform2">
                                <h3 class="regheader">User Information</h3>
                                <div class="row">
                                    {{!-- First Column --}}
                                    <div class="col">
                     
                                            <div class="form-group">
                                                {{!-- First Name --}}
                                                <label>First Name</label>
                                                <input name="first_name" id="first_name"  pattern="^[A-Za-z\s]+$" placeholder="First Name" type="text" class="form-control" minlength="2" maxlength="15" required/><br>

                                                {{!-- Last Name --}}
                                                <label>Last Name</label>
                                                <input name="last_name" id="last_name" pattern="^[A-Za-z\s]+$" placeholder="Last Name" type="text" class="form-control" minlength="2" maxlength="15" required/><br>

                                                {{!-- Email --}}
                                                <label>Email</label>
                                                <input name="email" id="email" type="email" placeholder="Email" class="form-control" required/><br>
                                            
                                                {{!-- Mobile Number --}}
                                                <label>Mobile Number</label>
                                                <input name= "mobileno" id="mobileno" type="tel" placeholder="09 _ _ _ _ _ _ _ _ _" pattern="[0]{1}[9]{1}[0-9]{9}" class="form-control" maxlength="11" required/><br>
                
                                            </div>
                                    </div>
                                    {{!-- Second Column --}}
                                    <div class="col">
                                            <div class="form-group">

                                                {{!-- Usertype --}}
                                                <label for="usertype">User Type</label>
                                                     <select id="usertype" name="usertype" class="form-control" onchange="switchtypes(); disableopt();">
                                                        <option value="Branch Manager">Branch Manager</option>
                                                        <option value="Admin">Admin</option>
                                                        <option value="Clerk">Clerk</option>
                                                    </select><br>
               
                                                {{!-- Branches --}}
                                                <label for="branch">Branch Assigned</label>
                                                    <select id="branch" name="branch" class="form-control">
                                                        {{!--{{> branchList}}--}}
                                                        <option id="branchselectA" value="Branch A">Branch A</option>
                                                        <option id="branchselectB" value="Branch B">Branch B</option>
                                                        <option id="branchselectC" value="Branch C">Branch C</option>
                                                        <option id="branchselectM" value="Managerial Branch" disabled>Managerial Branch</option>
                                                        <option id="branchselectCe" value="Central Branch" disabled>Central Branch</option>
                                                    </select><br>

                                                {{!-- Password --}}
                                                <label>Password</label>
                                                <input name="password" id="password" placeholder="Password" type="password" class="form-control" minlength="3" maxlength="20" required name=up/><br>

                                                {{!-- Confirm Password --}}
                                                <label>Confirm Password</label>
                                                <input name="confirmpassword" id="confirmpassword"placeholder="Confirm Password" type="password" class="form-control" required  required name=up2/><br>
                                            </div>
                                    </div>
                                  </div>
                            </div>
                            
                        </div>

                          {{!-- SUBMIT CANCEL --}}
                                <div class="row" style="margin-top: 2rem;">
                                    <div class="col-md-6">
                                        
                                    </div>
                                    <div class="col-md-6">
                                        <div class="text-md-right dataTables_filter" id="dataTable_filter">
                                            <label>
                                                <a href="/manageusers" class="btn btn-secondary btn-sm" type="button">Cancel</a>
                                                <button class="btn btn-primary btn-sm" id="addUser"type="submit">Register</button>
                                            </label>
                                        </div>
                                    </div>
                                </div>   
                                  {{!-- END OF SUBMIT CANCEL --}}

                     </form>      
                    </div>
                    {{!-- END OF REGUSER --}}

                       
                    </div>
                    {{!-- END TAB CONTENT --}}
                   
                    <div class="card-body"></div>
                </div>
                <div class="card shadow"></div>
            </div>
            <br><br>

    <script src="https://code.jquery.com/jquery-3.4.1.min.js" type="text/javascript"></script>
    <script type="text/javascript">

     function forceKeyPressUppercase(e){
                var charInput = e.keyCode;
                if((charInput >= 97) && (charInput <= 122)) { // lowercase
                if(!e.ctrlKey && !e.metaKey && !e.altKey) { // no modifier key
                var newChar = charInput - 32;
                var start = e.target.selectionStart;
                var end = e.target.selectionEnd;
                e.target.value = e.target.value.substring(0, start) + String.fromCharCode(newChar) + e.target.value.substring(end);
                e.target.setSelectionRange(start+1, start+1);
                 e.preventDefault();
                }
                }   
            };
            
           document.getElementById("first_name").addEventListener("keypress", function(e) {
            
            if(this.selectionStart == 0) {
            // uppercase first letter
            forceKeyPressUppercase(e);
            } else {
            // lowercase other letters
            forceKeyPressLowercase(e);
            }
            
            }, false);

            document.getElementById("last_name").addEventListener("keypress", function(e) {
            
            if(this.selectionStart == 0) {
            // uppercase first letter
            forceKeyPressUppercase(e);
            } else {
            // lowercase other letters
            forceKeyPressLowercase(e);
            }
            
            }, false);

            $(function () {

                $("#mobileno").on("keypress keyup blur",function (event) {    
                    $('#mobileno').val();
                        if ((event.which < 48 || event.which > 57)) {
                        event.preventDefault();
                        }
                    }); 
            });

            function switchtypes() {
                if (document.getElementById('usertype').value == 'Admin')
                document.getElementById("branch").value = 'Managerial Branch';
                else if (document.getElementById('usertype').value == 'Clerk')
                document.getElementById("branch").value = 'Central Branch';
                else
                document.getElementById("branch").value = 'Branch A';
                
            };

            function disableopt(){
                if(document.getElementById('usertype').value=='Admin')
                {document.getElementById('branchselectA').disabled=true;
                document.getElementById('branchselectB').disabled=true;
                document.getElementById('branchselectC').disabled=true;
                document.getElementById('branchselectCe').disabled=true;
                document.getElementById('branchselectM').disabled=false;
                }

                else if(document.getElementById('usertype').value=='Clerk')
               {document.getElementById('branchselectA').disabled=true;
                document.getElementById('branchselectB').disabled=true;
                document.getElementById('branchselectC').disabled=true;
                document.getElementById('branchselectCe').disabled=false;
                document.getElementById('branchselectM').disabled=true;
                }

               else if(document.getElementById('usertype').value=='Branch Manager')
                {document.getElementById('branchselectA').disabled=false;
                document.getElementById('branchselectB').disabled=false;
                document.getElementById('branchselectC').disabled=false;
                document.getElementById('branchselectCe').disabled=true;
                document.getElementById('branchselectM').disabled=true;
                }
            };


    </script>

    <script>
        function sortTable(n) {
        var table, rows, switching, i, x, y, shouldSwitch, dir, switchcount = 0;
        table = document.getElementById("usersTable");
        switching = true;
        //Set the sorting direction to ascending:
        dir = "asc"; 
        /*Make a loop that will continue until
        no switching has been done:*/
        while (switching) {
            //start by saying: no switching is done:
            switching = false;
            rows = table.rows;
            /*Loop through all table rows (except the
            first, which contains table headers):*/
            for (i = 1; i < (rows.length - 1); i++) {
            //start by saying there should be no switching:
            shouldSwitch = false;
            /*Get the two elements you want to compare,
            one from current row and one from the next:*/
            x = rows[i].getElementsByTagName("TD")[n];
            y = rows[i + 1].getElementsByTagName("TD")[n];
            /*check if the two rows should switch place,
            based on the direction, asc or desc:*/
            if (dir == "asc") {
                if (x.innerHTML.toLowerCase() > y.innerHTML.toLowerCase()) {
                //if so, mark as a switch and break the loop:
                shouldSwitch= true;
                break;
                }
            } else if (dir == "desc") {
                if (x.innerHTML.toLowerCase() < y.innerHTML.toLowerCase()) {
                //if so, mark as a switch and break the loop:
                shouldSwitch = true;
                break;
                }
            }
            }
            if (shouldSwitch) {
            /*If a switch has been marked, make the switch
            and mark that a switch has been done:*/
            rows[i].parentNode.insertBefore(rows[i + 1], rows[i]);
            switching = true;
            //Each time a switch is done, increase this count by 1:
            switchcount ++;      
            } else {
            /*If no switching has been done AND the direction is "asc",
            set the direction to "desc" and run the while loop again.*/
            if (switchcount == 0 && dir == "asc") {
                dir = "desc";
                switching = true;
            }
            }
        }
        }

        function filter() {
        var input, filter, table, tr, td, i, txtValue;
        input = document.getElementById("filter");
        filter = input.value.toUpperCase();
        table = document.getElementById("usersTable");
        tr = table.getElementsByTagName("tr");
        for (i = 0; i < tr.length; i++) {
            td = tr[i].getElementsByTagName("td")[2];
            if (td) {
            txtValue = td.textContent || td.innerText;
            if (txtValue.toUpperCase().indexOf(filter) > -1) {
                tr[i].style.display = "";
            } else {
                tr[i].style.display = "none";
            }
            }       
        }
        }
    </script>

            